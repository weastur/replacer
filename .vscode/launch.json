{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "replacer",
      "type": "go",
      "request": "launch",
      "mode": "exec",
      "program": "${workspaceFolder}/replacer",
      "args": [
        "ensure"
      ],
      "preLaunchTask": "build"
    }
  ]
}
